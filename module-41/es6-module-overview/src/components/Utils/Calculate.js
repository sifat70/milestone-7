const add = (first, second) => {
    return first + second
}

const multiply = (first, second) => {
    return first * second;
}

const theFirstNumberByTheSecondNumber = (first, second) =>{
    return first / second
}


export { add, multiply, theFirstNumberByTheSecondNumber }

// export default add;
// const calculator = {add: add};
// export default calculator